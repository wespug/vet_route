import 'package:flutter/material.dart';

class AppColors {
  // Cores Principais do Vet Route
  static const Color primary = Color(0xFF3F51B5); // Indigo (Base)
  static const Color secondary = Color(0xFF009688); // Teal (Clínica)
  static const Color accent = Color(0xFFFF9800); // Orange (Motoboy)

  // Cores de Apoio e Neutras
  static const Color background = Color(0xFFF4F6F9);
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
}
